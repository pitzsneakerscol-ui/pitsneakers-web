Coloca aquí las fotos de cada producto, en una subcarpeta por producto,
por ejemplo `air-max-97-silver-bullet/1.jpg`. Luego referencia esas rutas
en `src/data/products.json` bajo el campo `images`. Ver el README principal
del proyecto para más detalle.
